# Repos externos de referencia (clones de terceros, no son parte del proyecto)
repos/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/

# Runtime / datos locales
mvp/cache/
*.log

# Secrets. The example file is committed; the real one never is.
.env
.env.*
!.env.example

# Editor / SO
.vscode/
.idea/
Thumbs.db
desktop.ini

# Agent worktrees: copies of the repository, not source.
.claude/

# Photographs taken while measuring. They are evidence for a report and for the
# eye, not source, and they are re-takeable by running the probe beside them —
# which is the half that is versioned (decision 5.3).
research/shots/
